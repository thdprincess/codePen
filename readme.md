# CodePen 가이드

## 1. CodePen이 뭔가요?

CodePen은 **브라우저에서 바로 HTML / CSS / JavaScript를 실행해볼 수 있는 온라인 편집기**입니다.

설치:

- 필요 없음
- 회원가입 없이도 사용 가능 (저장은 로그인 필요)

주 용도:

- 애니메이션 테스트
- 라이브러리 실험 (Paper.js, Three.js 등)
- 코드 공유

---

## 2. 기본 화면 구조

CodePen 화면은 4칸으로 나뉩니다.

1. HTML 패널
2. CSS 패널
3. JS 패널
4. Preview (결과 화면)

작성 즉시 Preview에 반영됩니다.

---

## 3. 가장 중요한 설정 (외부 라이브러리)

### 3.1 JS 라이브러리 추가

Paper.js를 쓰려면 반드시 이 설정을 해야 합니다.

1. JS 패널 오른쪽 상단 ⚙ 아이콘 클릭
2. **JavaScript → Add External Scripts/Pens**
3. 아래 주소 추가

```
https://unpkg.com/paper@0.12.15/dist/paper-full.min.js
```

1. Save & Close

---

## 4. Paper.js 최소 템플릿 (CodePen용)

### HTML

```html
<canvasid="canvas"resize></canvas>
```

### CSS

```css
html,body {
	margin:0;
	padding:0;
	height:100%;
}

canvas {
	width:100%;
	height:100%;
	display: block;
}
```

### JS

```jsx
paper.setup(document.getElementById('canvas'));

var path =new paper.Path({
	strokeColor:'blue'
});

path.add([0,200]);
path.add([500,200]);

paper.view.onFrame =function (event) {
  path.segments[1].point.y = 200 +Math.sin(event.time *2) *40;
};
```

이 상태에서 Run 버튼 없이 바로 실행됩니다.

---

## 5. 기존 Pen 가져와서 쓰기 (가장 쉬움)

### 5.1 검색 방법

CodePen 상단 검색창에서:

```
paper.js wave
paperjsanimation
liquidbackground
```

### 5.2 Fork 사용

마음에 드는 Pen 발견 시:

1. Fork 버튼 클릭
2. 내 계정으로 복사
3. 자유롭게 수정

원본은 절대 망가지지 않습니다.

---

## 6. CodePen에서 “고쳐야 할 것만” 고치는 법

### 색상 바꾸기

```jsx
path.fillColor ='#46A5C4';
```

### 속도 조절

```jsx
Math.sin(event.time *1.2)
```

### 파도 높이

```jsx
*60
```

### 점 개수 (부드러움)

```jsx
for (var i =0; i <30; i++)
```

숫자만 만져도 결과가 크게 바뀝니다.

---

## 7. 디버깅 방법 (필수)

### 콘솔 열기

Preview 창 클릭 → F12 → Console 탭

자주 나는 오류:

- paper is not defined
    
    → 라이브러리 추가 안 됨
    
- canvas id 불일치
    
    → HTML과 JS의 id 확인
    

---

## 8. CodePen에서 작업할 때 좋은 습관

- 작동 확인 → Fork
- 숫자 하나씩만 수정
- 안 되면 되돌리기 (Ctrl+Z)
- 라이브러리는 JS 설정에서만 관리

---

## 9. 언제 CodePen이 최고인가

- 빠른 테스트
- 라이브러리 학습
- 애니메이션 튜닝
- 포트폴리오 데모

반대로:

- 대규모 프로젝트
- 빌드/배포
- 복잡한 상태 관리

이런 건 로컬 개발이 낫습니다.

---

## 10. 요약

- CodePen은 “실험용 놀이터”
- 외부 라이브러리 설정이 핵심
- Fork → 숫자 수정이 가장 쉬운 사용법
- Paper.js 연습에 최적

---

## CodePen 코드, 포트폴리오에 써도 될까?

### ✔️ **가능한 경우 (많음)**

아래 중 하나라도 해당되면 **포폴 사용 OK**인 경우가 많아요:

1. **MIT / Apache / GPL 같은 오픈소스 라이선스**
    - 상업적 사용 가능
    - 수정해서 써도 됨
2. CodePen 설명에
    
    > “Feel free to use”, “Free to use”, “MIT License”
    > 
    > 
    > 이런 문구가 있음
    > 
3. **개인 포트폴리오 / 학습용**으로 사용

👉 이 경우엔 **출처만 남기면** 거의 문제 없음

```html
<!-- Animation inspired by @username on CodePen -->
```

---

### ⚠️ **주의해야 하는 경우**

다음이면 조심해야 해요:

1. **라이선스 명시가 아예 없음**
    - 기본적으로 저작권은 작성자에게 있음
2. 설명에
    - “Do not use”
    - “All rights reserved”
    - “No commercial use”
3. **그대로 복붙 + 내 작업처럼 소개**

👉 이런 경우는 **허락 받거나**,

👉 **영감을 받았다고 명확히 밝히기**가 안전

```jsx
/*
	Interactive Typography Hero
	Inspired by a CodePen demo
	Customized and adapted for personal portfolio use
*/
```

---

## 포트폴리오에서 가장 좋은 사용 방식 (추천)

### 1. “활용 + 수정” 방식

- 색상 변경
- 인터랙션 변경
- 구조 일부 리팩토링
- GSAP → 다른 애니메이션 방식으로 바꾸기

👉 그리고 설명에:

> “CodePen 예제를 참고하여 인터랙션과 구조를 커스터마이징했습니다.”
> 

---

### 2. 출처 명시 (진짜 중요)

포폴에 이렇게만 적어도 인상 좋아요:

> Animation based on a CodePen by @username, customized and extended.
> 

이건 **마이너스가 아니라 플러스**입니다.

“저작권 감각 있는 사람”으로 보여요 👍

---

## 이런 건 피하자

- CodePen 메인 화면에 있는 화려한 데모를
    
    👉 **100% 그대로** → “내가 만든 것”처럼
    
- 상업 서비스(유료 제품, 클라이언트 납품)에
    
    👉 라이선스 확인 없이 사용
    

---

## 💡 한 줄 결론

> 포트폴리오 용도라면 대부분 OK,
대신 라이선스 확인 + 출처 명시는 필수!
>